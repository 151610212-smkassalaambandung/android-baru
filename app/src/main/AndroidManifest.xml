<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="assalaam.istigosah.id.istigosah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PengertianIstigosah" />
        <activity android:name=".TataCara" />
        <activity android:name=".bentukbentukistigosah" />
        <activity android:name=".tujuanistigosah" />
        <activity android:name=".home">

        </activity>
        <activity android:name=".Doaistigosah" />
        <activity android:name=".menu">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>
    </application>

</manifest>